services:
  app:
    build:
      context: .
      target: development
    env_file:
      - .env
    ports:
      - '${PORT:-3000}:3000'
    volumes:
      - ./:/app
      - /app/node_modules
    command: npm run dev
    restart: unless-stopped
