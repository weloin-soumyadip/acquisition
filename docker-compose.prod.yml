services:
  app:
    build:
      context: .
      target: production
    env_file:
      - .env.production
    environment:
      NODE_ENV: production
      NEON_USE_LOCAL: 'false'
    ports:
      - '${PORT:-3000}:3000'
    command: node src/index.js
    restart: unless-stopped
